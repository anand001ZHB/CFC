import { FilterEstablishmentsPipe } from './filter-establishments.pipe';

describe('FilterEstablishmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstablishmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
