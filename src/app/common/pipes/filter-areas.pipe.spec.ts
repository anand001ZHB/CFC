import { FilterAreasPipe } from './filter-areas.pipe';

describe('FilterAreasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAreasPipe();
    expect(pipe).toBeTruthy();
  });
});
